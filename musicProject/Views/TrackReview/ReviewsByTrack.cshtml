@model musicProject.Models.TrackModels.TrackWithReviews

    <form>
        <input asp-for="@Model.Id" hidden />
    </form>

<h1><i>@Model.Title</i> Reviews</h1>

<table class="table table-striped table-dark">
    <thead>
        <tr>
            <th>Rating</th>
            <th>User</th>
            <th>Content</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var review in Model.TrackReviews)
        {
            <tr>
                <td>@review.Rating</td>
                <td>@review.User.UserName</td>
                <td><a asp-controller="TrackReview" asp-action="Detail" asp-route-id="@review.Id">Details</a> </td>
            </tr>
        }
    </tbody>
</table>

<hr />
<a asp-controller="TrackReview" asp-action="Review" asp-route-id="@Model.Id">Review This Track</a> |
<a asp-action="NewIndex" asp-controller="TrackReview">Back To List</a>